# Secrets — never commit
keys/
.env
.env*
*.keypair.json
*-keypair.json
id.json

# Build / tooling
.anchor
target
test-ledger
.surfpool
node_modules
.next
dist
coverage
*.tsbuildinfo
**/*.rs.bk
.DS_Store
.yarn

# Local reference checkouts (large, fetched by scripts)
vendor/

# Local tooling and private notes
.claude/
__pycache__/
private/
